# TODO

MVP として初期利用できる状態にはなっているため、以下は次回以降の実装に回す。

## 優先度高

- build 前に `dist` を掃除する、または TypeScript 設定変更後に古い生成物が残らないようにする。
- README に具体的な MCP クライアント設定例と、ルートディレクトリの安全境界に関する説明を追加する。

## 後続

- `write_file` と `append_file` で存在しない親ディレクトリを作成するか、現在の「親ディレクトリは既存必須」という仕様を維持するか決める。
- 必要に応じて `search_files` にファイル名フィルタ、拡張子フィルタ、1 行または 1 ファイル内の複数マッチ返却を追加する。
- `files`、`prepack`、最終的な npx コマンド名など、パッケージ公開向けの設定を追加する。
- symlink されたワークスペース、symlink された親ディレクトリ、不正な UTF-8、大きなファイル、FIFO、デバイスファイルの安全性テストを追加する。
